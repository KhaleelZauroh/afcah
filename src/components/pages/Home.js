import React from 'react'

/* make an api fetch request from component did mount oruse effect to render (do the same for other pages) */
export default function Home() {
    return (
        <div>
            Home
        </div>
    )
}
